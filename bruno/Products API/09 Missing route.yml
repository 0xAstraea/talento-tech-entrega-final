info:
  name: 09 Missing route
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:3000/missing-route
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
