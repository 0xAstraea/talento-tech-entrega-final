info:
  name: 08 Delete product
  type: http
  seq: 9

http:
  method: DELETE
  url: http://localhost:3000/api/products/{{product_id}}
  headers:
    - name: Authorization
      value: "{{bearer_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
