info:
  name: 04 Get product by id
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/api/products/{{product_id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
